# dsh-geodesy bundle patch: mounts this plugin into the profile as one layer row.
# Configuration defaults live in the plugin schema (Config export).
- insert:
    - id: geodesy
      name: dsh-geodesy
